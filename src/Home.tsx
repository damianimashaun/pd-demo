import React from 'react';
import { View, Text } from 'react-native';
import { strings } from '../config';

const Home = () => (
  <View>
    <Text>{strings.aPass}</Text>
  </View>
);

export default Home;
